#SPDX-License-Identifier: MIT-0
---
galaxy_info:
  role_name: vector
  description: "Installs Vector (Datadog) as a DaemonSet - Rust-based alternative to Fluent Bit for CPU/Disk I/O comparison (Stack 3)"
  license: MIT-0
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - all

# Vector wymaga backendu logów (Loki) do działania
dependencies:
  - role: loki
