- name: Deploy stack 3 - Vector (Rust log agent) comparison to Fluent Bit (Cilium + OTel + Jaeger + Vector)
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  vars_files:
    - ../secret
  roles:
    - cilium
    - prometheus_stack
    - loki
    - vector
    - otel_operator
    - otel_stack
    - jaeger_v2
    - google_demo
