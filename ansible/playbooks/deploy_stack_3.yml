- name: Deploy stack 3 - Vector (Rust log agent) comparison (Cilium + OTel + Jaeger + Vector)
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  roles:
    - cilium
    - prometheus_stack
    - loki
    - vector
    - otel_operator
    - otel_stack
    - jaeger_v2
    - google_demo
