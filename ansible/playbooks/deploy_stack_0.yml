- name: Deploy stack 0 - Baseline (Google Boutique only, no observability)
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  roles:
    - prometheus_stack
    - google_demo
