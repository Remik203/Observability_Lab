- name: Deploy stack 0 - Baseline (Google Boutique only, no observability)
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  vars_files:
    - ../secret
  roles:
    - prometheus_stack
    - google_demo
