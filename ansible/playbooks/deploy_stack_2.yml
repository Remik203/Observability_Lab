- name: Deploy stack 2 - Cilium eBPF + OTel Instrumentation
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  roles:
    - cilium
    - prometheus_stack
    - otel_operator
    - otel_stack
    - jaeger_v2
    - google_demo
