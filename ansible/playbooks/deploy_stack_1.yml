- name: Deploy stack 1 - Standard observability stack (Prometheus + OTel + Loki)
  hosts: k3s_cluster
  gather_facts: true
  vars:
    stack_state: present
  roles:
    - prometheus_stack
    - loki
    - fluent_bit
    - otel_operator
    - otel_stack
    - istio
    - jaeger_v2
    - google_demo
