- name: Deploy stack 1 - Standard observability stack (Prometheus + OTel)
  hosts: k3s_cluster
  vars:
    stack_state: present
  roles:
    - prometheus_stack
    - otel_operator
    - otel_stack
    - istio
    - jaeger_v2
    - google_demo
